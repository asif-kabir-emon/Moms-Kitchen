import React from "react";
import useTitle from "../../Hooks/useTitle";

const Register = () => {
  useTitle("Register");
  return (
    <div>
      <h1>Register</h1>
    </div>
  );
};

export default Register;
