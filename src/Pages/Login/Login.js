import React from "react";
import useTitle from "../../Hooks/useTitle";

const Login = () => {
  useTitle("Login");
  return (
    <div>
      <h2>login</h2>
    </div>
  );
};

export default Login;
