import React from "react";
import useTitle from "../../Hooks/useTitle";

const Services = () => {
  useTitle("Services");
  return (
    <div>
      <h2>Services</h2>
    </div>
  );
};

export default Services;
